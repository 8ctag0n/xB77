
*/*.vault
round2
zig-out/
.zig-cache/
*.key
*_vault.csv
MEGAPLAN_PRIVATE.md
loght_forensics/
*.txt
cache
.env
**/target/
sdk/src/artifacts/
.localnet/
.devnet/
.localnet-ledger/
contracts/target/
onchain/target/
target/
test-ledger/
.xb77
*private/
node_modules
*.sw?
# Onchain builds
**/*.so
**/*.ds_store
*.db
*.log
mcp/listener.pid
demo/keys/*.json
demo/logs/
demo/data/
round2/

# ZK artifacts (regenerable via `podman run --rm xb77-zk:latest prove`)
circuits/zk_receipt/proofs/
circuits/zk_receipt/target/
*.tr

# Session briefs (ad-hoc handoff notes, not source of truth)
BRIEF_*.md

# Local-only brainstorming/spec artifacts (never commit)
docs/superpowers/

# Local-only scratch dir (NOT for the public repo).
# Note: `___legacy/` is intentionally tracked so the v3 cleanup can drop
# it in a single commit, leaving a clear audit trail of what was archived.
2organize-after-hackahton/

# Test-generated artifacts
circuits/state_anchor/Prover.toml
circuits/state_anchor/target/
circuits/state_anchor/proofs/

# Wrangler / Cloudflare Pages local cache
.wrangler/
**/.wrangler/
.cf_deploy_summary
webapp_deploy/remotion/out/
