services:
  noir-dev:
    build:
      context: .
      dockerfile: Containerfile
    image: xb77-noir:local
    container_name: noir-dev
    network_mode: "host"
    working_dir: /work
    entrypoint: ["bash", "-lc"]
    command: ["tail -f /dev/null"]
    volumes:
      - ${NOIR_REPO}:/work
