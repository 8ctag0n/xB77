[package]
name = "xb77_zk_verifier"
version = "0.1.0"
edition = "2021"

[features]
no-entrypoint = []

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "~3.0"
solana-system-interface = { version = "*", features = ["bincode"] }
